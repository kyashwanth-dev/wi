name: chatridge
description: Offline local WiFi messaging app (Chatridge) — scaffold
publish_to: 'none'
version: 0.1.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.5.0
  shared_preferences: ^2.1.1
  file_picker: ^10.3.3
  path: ^1.8.3
  path_provider: ^2.0.15
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^5.2.1
  permission_handler: ^12.0.1
  flutter_lints: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
